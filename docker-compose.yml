services:
  core-fe:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: core-fe
    volumes:
      - .:/app
      - core-fe-node-modules:/app/node_modules
    ports:
      - "5173:5173"
    command: yarn dev --host
 
volumes:
  core-fe-node-modules: